/*
To avoid from using magical strings, I keep then as constants
this is the name of the event name which we're using in emitting socket.io message to frontend
*/
export const FAKE_EXCHANGE_CREATED = "FAKE_EXCHANGE_CREATED";
